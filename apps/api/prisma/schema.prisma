generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Stage 1 keeps Prisma connected without business models.
// Bull and WeightRecord will be added in Stage 2.
