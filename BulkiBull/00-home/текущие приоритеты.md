---
tags:
  - priorities
  - codex
date: 2026-04-15
project: Bulki Bull
---

# Текущие приоритеты

## Сейчас

- Держать Obsidian vault в папке `BulkiBull/`, а инструкции для Codex в корневом `AGENTS.md`.
- Перед крупными задачами читать [[index|Bulki Bull Knowledge Vault]] и релевантные заметки из `atlas/` и `knowledge/`.
- Сохранять важные решения и баги отдельными заметками с wiki-ссылками.

## Следующий технический этап

- Stage 2: добавить Prisma-модели `Bull` и `WeightRecord`.
- Создать миграции и seed-данные для локальной разработки.
- Добавить REST CRUD endpoints в `apps/api`.
- Вынести общие DTO/типы в `packages/shared`, чтобы API и mobile не расходились.

## Следующий продуктовый этап

- Stage 3: заменить placeholder на мобильные MVP-экраны списка, карточки и формы.
- Stage 4: добавить работу с фото, когда базовый учет бычков стабилизируется.

## Проверки перед завершением задач

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm run prisma:validate -w apps/api` после изменений Prisma
