---
tags:
  - priorities
  - codex
date: 2026-04-17
project: Bulki Bull
---

# Текущие приоритеты

## Сейчас

- Держать Obsidian vault в папке `BulkiBull/`, а инструкции для Codex в корневом `AGENTS.md`.
- Перед крупными задачами читать [[index|Bulki Bull Knowledge Vault]] и релевантные заметки из `atlas/` и `knowledge/`.
- Сохранять важные решения и баги отдельными заметками с wiki-ссылками.
- Считать исходный код и README главным источником истины: MVP после этапов 1-5 уже реализован.
- Учитывать, что mobile уже выбирает фото из галереи телефона и умеет снимать его камерой, а до backend upload временно хранит сжатый `data:image/...` в `photoUrl`.
- Учитывать, что удаление бычка уже доступно из карточки и требует подтверждения, см. [[удаление бычка в MVP требует подтверждения и удаляет историю веса]].
- Помнить, что desktop web для `Camera.takePhoto` использует [[desktop web использует PWA Elements для Camera.takePhoto]], а iOS Simulator не дает надежной проверки live preview камеры.
- Если mobile UI уже обновился в web, но не обновился в iOS Simulator, использовать сценарий из [[Capacitor copy и sync не обновляют уже установленный iOS build в симуляторе]].
- Помнить, что API уже принимает увеличенный photo payload, синхронизированный с shared лимитом `PHOTO_DATA_URL_MAX_LENGTH`.

## Следующая техническая итерация

- Добавить backend API-тесты для CRUD бычков, истории веса и вычисления `currentWeight`.
- Добавить e2e smoke-тест основного mobile flow.
- Заменить временное хранение сжатого `data:image/...` в `photoUrl` на backend upload в локальное dev-хранилище с точкой расширения под S3/MinIO.
- Подумать о script вроде `ios:rebuild` для полного `build -> cap sync ios -> xcodebuild -> simctl install/launch`, чтобы быстрее обновлять приложение в симуляторе.
- Подготовить production/deploy notes и env-документацию.

## Следующая продуктовая итерация

- Улучшить UX ошибок, когда API недоступен.
- Продумать pagination или lazy loading для большого списка бычков.
- Решить, должен ли компактный поиск в списке бычков сохранять строку после закрытия панели.
- Удаление бычка уже есть с подтверждением; отдельно решить, нужно ли удаление записи веса и где его показывать.
- Не добавлять авторизацию, офлайн, учет кормов, продажи или аналитику без отдельного решения.

## Проверки перед завершением задач

- `npm run build`
- `npm run typecheck`
- `npm run lint`
- `npm run format:check`
- `npm run prisma:validate -w apps/api` после изменений Prisma
